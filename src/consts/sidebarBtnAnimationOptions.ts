export const sidebarBtnAnimationOptions = {
    initial: { x: -60, y: -100, transition: { duration: 0, ease: 'easeOut' }},
    animate: { x: 0, transition: { duration: 0.4, ease: 'easeOut' }},
    exit: { x: -60, transition: { duration: 0.4, ease: 'easeOut' }}
};